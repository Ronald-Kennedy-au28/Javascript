// EVENTS**

// we learnt a little about the DOM tree
// and how we select the html selectors
// like we do in Css
// we learnt about events but not sure of the 
// functions,
// However, we need to go again or refer different
// source for Events

// three ways to select dom element(html element);
// 1. Id = sok
// 2. Class = dok
// 3. tag = mok

// so to select the a dom element by Id
// var byId = document.getElementById("sok");
// Ids are unique hence , there is no confusion
// we can access easily

// var byClass = document.getElementsByClass("dok");
// Since classname "dok" can be in many places in html
// the byClass creates an array, so to access those we use indexing 
// byClass[0].addEventListener("onclick");

// the above applies to tagname name as well 
// just like class , taganames can be in many places in dom.

